# portfolio-v2

This repository is initialized and ready for future project files.

## Notes

- Changes are committed locally on the current branch.
- Pull requests should be created using Codex's built-in GitHub integration.
- Do not manually run `git remote add` or `git push` in this workflow.
